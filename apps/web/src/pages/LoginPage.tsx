import { PageSection } from "../components/common/PageSection";

export function LoginPage() {
  return (
    <PageSection title="Discord 로그인" description="OAuth 연동은 다음 단계에서 구현합니다.">
      <div className="placeholder-card">로그인 버튼과 서버 멤버 확인 로직이 들어갈 자리입니다.</div>
    </PageSection>
  );
}
