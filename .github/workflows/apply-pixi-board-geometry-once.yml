name: Apply Pixi Board Geometry Once

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. The board geometry refactor was reverted after verification."
